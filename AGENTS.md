# 本项目 Codex 统一规则

## 1. 项目目标与当前阶段

本项目参加第五届中国研究生金融科技创新大赛“揭榜挂帅”赛道第27题：

> 碳迹可循，绿贷智评：基于多维度数据与行业标准的企业转型金融评估系统。

目标是形成可运行、可测试、可追溯的企业转型金融评估系统，以及精益画布、技术文档、原创性声明、竞赛报告、演示和答辩材料。系统应服务于转型主体识别、能耗与碳排放核算、转型计划可信度评估、风险提示、证据追溯和企业改进建议，不自动作出授信通过或拒绝决定。

当前处于“材料审阅与总体规划阶段”。在用户明确发送完全一致的口令“开始开发MVP”之前，任何 Codex 任务都不得编写或修改程序代码、制作网页或桌面软件、安装依赖、调整开发环境、使用模拟数据跑出项目结论，或启动程序开发型子Agent。

## 2. 任务开始前必须读取的文件

每个任务开始时，先按以下顺序读取：

1. `AGENTS.md`；
2. `README.md`；
3. `docs/PROJECT_CONTEXT.md`；
4. `docs/PROJECT_STATUS.md`；
5. `docs/PROJECT_PLAN.md`；
6. `docs/DECISIONS.md`、`docs/DATA_STATUS.md`、`docs/TEAM_ROLES.md`、`docs/TASK_BOARD.md`；
7. `docs/SYNC_WORKFLOW.md`、`docs/CODEX_ONBOARDING.md`、`handoffs/README.md`；
8. `企业转型金融智能评估系统-项目统领提示词-计划模式.md`（历史规划输入，仅作追溯，不重新执行）；
9. 与本次任务直接相关的原始材料：目标赛题书、参赛指南、精益画布模板、`图片信息/`，以及必要的政策、标准和文献。

本项目内的 `docs/PROJECT_CONTEXT.md` 是跨成员、跨Codex任务的主上下文，`docs/PROJECT_PLAN.md` 是唯一正式总体计划；如它们与较早的规划文字冲突，应先记录冲突并以最新确认决策为准，不得静默改写范围。

### 2.1 任务开始时的版本同步

- 先检查 `git status --short --branch` 和 `git remote -v`。
- 本项目实行“单向GitHub”：若翎维护的远端主仓库是正式版本源；吴、钟、刘、夏及其Codex只获取和读取，不向远端推送提交、创建远端分支或发起拉取请求。
- 若存在 `origin/main`，成员Codex只获取远端状态并比较本地版本；本地工作区干净且仅落后时，可使用快进方式同步后再开始任务。
- 若本地有未提交修改、分支分叉或同步可能覆盖成员成果，不得自动合并、重置或覆盖；先报告差异并保留现有工作。
- 若没有Git远端，检查成员是否收到若翎更新后的项目包或核心文档，并比较 `docs/PROJECT_STATUS.md` 的版本与日期。未取得新版文件时，只能说明当前依据版本，不能声称已同步最新状态。
- 完成同步检查后，重新读取 `docs/PROJECT_CONTEXT.md`、`docs/PROJECT_STATUS.md`、`docs/DECISIONS.md` 和 `docs/TEAM_ROLES.md`，再执行具体任务。

### 2.2 对话与任务边界

- 若翎长期保留少量固定Codex对话：`00_项目上下文整理与Codex交接包`已完成、可归档；`01_项目总控与决策`负责总体计划、重大决策、状态、成员成果审查、正式合并和Git操作；`02_软件开发`负责程序架构、开发、测试、封装和部署；`03_数据、政策与评分体系`负责正式数据审计、政策标准、行业规则和评分校准；`04_报告、PPT与答辩`暂不创建，待程序、数据和评分体系基本稳定后再建立。
- `01_项目总控与决策`不承担日常软件编码；`02_软件开发`只有收到完全一致的口令“开始开发MVP”后才可开始程序开发。
- `docs/TASK_BOARD.md`仅作为可选的简要待办清单，不是任务启动前置条件。任务编号、任务卡、按编号命名对话和独立编号目录均不作强制要求。
- 吴、钟、刘、夏不固定绑定岗位，可按实际需要承担政策、数据、评分、测试、报告、软件建议或其他工作。成员成果可直接以消息、文档、代码片段、数据结果或ZIP发送给若翎，无需固定交接结构。
- `handoffs/`保留为可选存档目录；`HANDOFF.md`、`SYNC_PROMPT.md`、ZIP交接包和“我要同步”口令均为可选工具，不是成员开始或完成工作的必要条件。
- `PROJECT_CONTEXT.md`、`PROJECT_STATUS.md`、`DECISIONS.md` 和 `PROJECT_PLAN.md` 原则上由 `01_项目总控与决策` 维护。成员成果由该对话先比较、分类和提出采纳建议，待若翎确认后再写入正式项目。

### 2.3 子Agent规则

- 默认不启用子Agent，也不因为建立多个Codex对话而自动调用子Agent。
- 只有若翎明确要求，或任务明显适合并行只读分析时，才可调用子Agent。适用场景包括大规模文献检索、政策资料归纳、代码只读检查、测试结果分析和多方案比较。
- 涉及正式代码或核心项目文件的写入，原则上由当前主对话统一完成，避免并行修改冲突。

## 3. 数据边界

已取得命题方提供的配套模拟数据文件，但尚未取得独立数据字典、训练/测试划分或正式评分标签。该数据是脱敏、模拟生成的比赛开发测试数据，不是真实企业业务数据，也不能作为真实企业融资认定、碳核算或授信依据。赛题材料中未被该文件证实的数据规模、字段和数据资源描述，仍只能写成“赛题描述”或“待领取/待确认”。

- 赛题描述包括：1000户企业、覆盖2024—2025年、11个行业、500余条问答语料和30组智能提问模拟对话；其中部分配套模拟数据文件已到达，但这些材料仍不等于真实企业业务数据或正式评分标签。
- 赛题书另述160家企业能耗、排放、经营数据和行业标准参数库；同样不得视为当前已取得的正式参赛数据。
- 不得虚构企业、字段值、标签、准确率、模型效果、评分结果、真实企业结果或用户满意度。
- 当前可以设计数据契约、字段建议、质量规则、指标框架和审计流程，但不能据此宣称模型达标。
- 当前配套工作簿的`基本信息`、`能耗信息`、`补充信息`是主要输入；`转型目录`是规则与知识来源；`转型规划结论`仅作参考结论和验证对照，不得作为同一模型的输入特征。
- 若后续确需使用模拟数据，必须先说明生成规则并取得用户确认；所有模拟数据和结果都要显式标注“模拟数据，不代表比赛正式数据”，且不得用于声称真实业务效果。
- 在配套模拟数据进入分析或开发流程前，必须先完成文件盘点、字段确认、质量检查、缺失/异常/重复分析、口径和单位核查、标签泄漏检查、处理链路记录和可用性分级；独立数据字典、训练/测试划分和正式评分标签到达后，才可重新校准权重、阈值、行业基准和模型。

## 4. 研究、政策与文献核验

政策、标准、因子、行业基准和文献结论必须来源可追溯。优先使用发布机构、监管部门、标准发布方、国际组织或正式出版物的原文，并记录标题、发布机构、版本/发布日期、生效期、适用地区和链接或仓库相对路径。

- 明确区分“已读取并核验”“已有来源但待复核”“专业判断初稿”“待正式数据确认”。
- 不把搜索摘要、经验图、往届作品或二手转载当作最终权威依据。
- 不虚构文献、DOI、页码、政策条款、行业系数或数据来源；无法取得全文时标记“待核”。
- 规则、权重、阈值和行业基准在正式数据、命题方标准和正式材料到达前只能作为专业判断初稿。
- 政策和标准若存在版本变化，必须记录有效日期，不能把旧版本直接当作当前适用版本。

## 5. 软件开发授权边界

在收到“开始开发MVP”前，只允许材料分析、只读检查、方案整理、规则需求、数据需求、测试设计、报告大纲和只读型评审。不得：

- 编写、修改、删除或批量重构 Python、TypeScript、SQL、配置和其他程序代码；
- 制作网页、桌面程序、安装包、数据库或可运行原型；
- 安装依赖、升级软件、修改 Python 或其他开发环境；
- 使用模拟数据制作所谓的完整系统成果；
- 启动承担程序开发工作的子Agent。

即使用户说“继续执行”“按照计划开展”或“实施计划”，也不能替代精确口令。收到精确口令后只需核对任务范围和当前状态即可按MVP实施；正式数据、数据授权、隐私或保密不是启动程序开发的前置条件。不得擅自删减“8行业框架＋2行业深验”。

## 6. 已确认技术边界

当前确认的技术路线包括：React、TypeScript、Vite、Ant Design、ECharts；Python、FastAPI、Pydantic、SQLAlchemy；桌面版SQLite、服务器版PostgreSQL；本地不可变原件目录与SHA-256；PyMuPDF/pdfplumber、python-docx、openpyxl、PaddleOCR；JSON/YAML行业模板；规则优先、可替换的视觉语言模型接口；Jinja2、ReportLab、python-docx报告生成；PyWebView与PyInstaller桌面封装；本地网页及Windows云服务器双部署。

系统产品中的外部大模型默认关闭，但研究和开发任务可按需要调用第三方模型，不再把数据上传、隐私或保密作为阻塞条件。调用时仍应记录服务商、模型、用途、失败回退和人工复核边界。模型不得直接决定授信。

## 7. Git、共享与文件管理

- 本项目是独立Git仓库，不得重新初始化、强制推送、重写历史或覆盖成员已有成果。
- 正式仓库为若翎维护的公开个人GitHub仓库：<https://github.com/RuoLing0802/transition-finance-assessment>。其他成员无需加入Collaborator，只通过公开地址 `clone`、`pull` 和读取；不得向远端推送提交、创建远端分支或发起拉取请求。
- 成员第一次使用时clone正式仓库；每次开始新工作前，先确认没有未保存的本地改动，再执行 `git pull --ff-only origin main` 并读取最新版项目文档。若有本地改动或分叉，先保留并报告，不得强行同步。
- 只有若翎或若翎明确授权的项目总控/集成Codex可以修改正式仓库、提交和推送。成员把成果直接发送给若翎，由 `01_项目总控与决策` 审查后决定是否纳入正式仓库。
- 公开仓库只纳入可公开分享的竞赛原始材料和项目治理文档；正式数据、未公开标签和原始数据、数据库、`.env`、API Key、密码及访问令牌不得上传到公开仓库。受控环境中的任务处理和外部服务调用仍按本文件记录来源、用途、失败回退和人工复核边界。
- 需要使用密钥时从本机或受控配置读取，除任务确有需要外，不在终端进度、普通日志或交付说明中重复打印完整密钥值，也不得将密钥写入公开仓库。
- 使用仓库相对路径；个人电脑绝对路径只用于本机操作，不写入需要跨成员共享的项目文档。
- 若发现已有工作区变更，先报告并保留，不得以“整理”为由清理。

## 8. Python与环境

本项目日常 Python 解释器使用 `/opt/anaconda3/bin/python`，依赖安装（仅在获得明确授权后）使用同一解释器的 `-m pip`。不要将裸 `python3`、`pip3` 或 `/usr/bin/python3` 作为默认环境；不要跨解释器搬运 `site-packages`。当前任务禁止安装依赖或调整环境。

## 9. 测试要求

所有未来开发或材料交付都必须区分“已验证”“未验证”“待用户确认”。

- 计算、规则、证据链、动态提问、报告、权限、日志、安全和跨平台部署都要有可重复的测试案例与验收标准。
- 赛题目标值不是实测结果。正式标签、金标准和真实用户测试缺失时，只能提交测试设计，不能宣布准确率、相关性、满意度、采纳率或性能达标。
- 赛题中的“单次评估≤3秒、并发≥100次/秒”只在测试口径明确后测量；必须披露硬件、并发模型、样本规模及是否排除OCR和报告生成。
- 文档检查至少包括：MVP范围、正式数据状态、团队分工、技术架构、路径可移植性、匿名性、引用状态和未完成事项。

## 10. 任务结束时的汇报与文档更新

每次任务结束必须说明：新增或修改的文件、完成内容、验证方式、未验证风险、待用户确认事项，以及是否改变了阶段状态。涉及项目决策、数据状态、风险、人员分工或计划的变化，必须同步更新对应的 `docs/` 文件。

成员任务结束时直接把成果、依据、已验证项、风险和待确认项发送给若翎即可；需要存档或材料较多时，可以自愿使用 `handoffs/`、`HANDOFF.md`、`SYNC_PROMPT.md` 或ZIP。成员不得提交或推送Git。`01_项目总控与决策`读取材料后，应区分新增、重复、冲突、过时和待确认内容，给出“建议采纳、部分采纳、暂不采纳”的审查结论，并在若翎确认后完成正式文档更新、检查、提交和推送。

本项目管理文档只记录事实、已确认方案和清楚标注的暂定方案。不得把计划写成已经完成的软件，不得把目标指标写成实测结果，不得在未获授权时进入程序开发。
