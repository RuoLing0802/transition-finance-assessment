# 项目状态

**版本**：v1.6
**最近更新时间**：2026-08-20
**当前阶段**：M3/M4已正式集成；钟同学政策规则交付已归档为M5候选知识源；M5专项开发说明待策划
**阶段锁**：M5不得自动启动；须先形成来源、版本、证据和隔离方案，经若翎确认并向02发送完全一致的开发口令后实施
**负责人**：若翎

## 当前结论

项目继续以“8行业框架＋2行业深验”为完整MVP范围。命题方配套模拟数据已经取得、登记并完成初步审计；该数据是脱敏、模拟生成的比赛开发测试数据，不是真实企业业务数据。M1—M4工程基线均已通过01审查。

正式基线已经具备配套数据自动加载/恢复、五表校验、企业关联、企业详情和能耗变化、数据质量、目录候选、参考结论隔离、多企业工作空间、单评估运行一企业、多模态适配、受控模型编排、React专业Agent工作台、LangGraph运行级状态编排、LangChain Core确定性节点包装、SQLite检查点、暂停/恢复、人工确认、离线回退、基础对比/输出和macOS桌面入口。

M4不包含知识检索、正式碳核算、行业对标、转型评分、信贷支持、Windows或报告扩展。M5知识检索尚未启动；钟同学交付提供了候选来源、条款、铜路径、行业覆盖和因子准入材料，但必须先统一来源ID、官方URL/原件、证据路径、适用边界、更新和参考结论排除规则。Windows EXE/MSI未在Windows环境构建验收，报告扩展也未启动。

## 本轮完成：M3/M4基线保持、政策规则材料接收

- 正式采纳M3 React、TypeScript、Vite、Ant Design和ECharts专业Agent工作台，保留左侧工作空间/运行、中间会话、右侧检查器和真实能力状态。
- 正式采纳M4两类运行级工作流、LangGraph状态图、LangChain Core确定性节点包装、SQLite持久检查点、暂停/恢复、人工确认和离线回退。
- 完成越级人工确认、空回答绕行、暂停来源状态、检查点乐观锁、参考层白名单和跨运行/新进程恢复整改。
- 工作流使用独立白名单分析视图；`reference_comparison`和`转型规划结论`不进入工作流输入、模型输入、检索、特征或标签。
- Python全量在受限沙箱中为`59 passed, 1 failed, 5 warnings`；唯一失败是沙箱禁止桌面烟测绑定本地端口，该用例在允许端口环境中为`1 passed`，分拆覆盖全部`60`项测试。前端为`3 test files / 5 tests passed`，类型检查和构建通过。
- M4仍是同步执行到等待点，不宣称已验证长任务实时中断、高并发性能或真实外部模型参与工作流效果。
- 新增[`M4_DEVELOPMENT_REVIEW.md`](M4_DEVELOPMENT_REVIEW.md) v1.0并更新开发审查、开发简报、总体计划、主上下文、决策和README。
- 接收并归档钟同学两份政策规则精简工作簿、Sheet清理清单和交付说明；文件结构、哈希、Sheet数量、状态分布和内部冲突已只读核验。
- 新增[`POLICY_RULES_REVIEW.md`](POLICY_RULES_REVIEW.md) v1.0：161条主规则候选、196条证据明细候选、43条铜路径、11行业覆盖和因子准入信息可用于M5准备；`source_id`冲突、个人绝对路径和来源链接不完整必须整改。
- 当前新增因子允许自动调用为0条；未启动知识检索、碳核算、行业对标、评分、Windows或报告扩展。
- 吴同学早期分工文件仅移入历史存档；其中开发未启动、固定岗位和把`转型规划结论`作为AI标签等旧表述不纳入正式计划或项目记忆。

## 当前正式技术状态

| 层次 | 已接入 | 尚未接入/未验证 |
|---|---|---|
| 前端 | React、TypeScript、Vite、Ant Design、ECharts，Codex式三栏工作台 | 大包分块优化、完整可访问性和干净机浏览器回归待后续 |
| 后端 | Python、FastAPI、Pydantic | SQLAlchemy、PostgreSQL未接入 |
| 持久化 | 原生`sqlite3`、本地应用数据目录、SHA-256、运行级工作流检查点与严格乐观锁 | 服务器数据库迁移和高并发性能测试未开始 |
| 数据处理 | `openpyxl`、五表校验、企业关联、质量提示 | 独立正式数据字典、正式标签和训练/测试划分仍缺失 |
| 多模态 | PDF/DOCX/图片适配、外部视觉模型接口 | 真实供应商完整联网验收、生产级OCR效果未验证 |
| Agent/工作流 | OpenAI兼容会话模型、用户本机模型配置、工具白名单、LangGraph、LangChain Core、人工确认、离线回退 | M5候选知识材料已到达但检索未接入；真实外部模型参与M4工作流的完整链路未验证 |
| 评估内核 | 能耗原始值变化、目录候选 | 正式碳核算、行业基准、转型行为、评分与信贷支持未接入 |
| 桌面 | PyWebView、PyInstaller、macOS本地构建基础 | Windows EXE/MSI、签名/公证和干净机分发未完成 |

安全限制：当前自定义模型API Key不返回前端或模型列表，但在本机应用SQLite中仍为明文存储。该方案仅作为本地MVP基线接受；正式分发前必须迁移到操作系统凭据存储并补充权限、迁移和删除测试。M4未扩大密钥暴露面。

## 数据使用边界

- `基本信息`、`能耗信息`、`补充信息`是程序输入层。
- `转型目录`是规则与知识来源，当前只有临时行级标识和候选匹配，正式来源、版本、生效期和稳定`rule_id`待确认。
- `转型规划结论`只作参考答案、验证对照或测试基准；不得进入同一模型的训练特征、推理特征、提示上下文或标签生成链路。
- 当前仍缺少独立数据字典、训练/测试划分、正式评分标签、组织/核算边界、Scope口径，以及可直接执行的完整排放因子和折标参数体系。
- 政策规则工作簿不是企业数据字典或评分标签；因子记录目前只作证据和待核验材料，0条允许自动调用。

## 钟同学政策规则交付审查分类

### 已确认可归档

- 两份精简工作簿分别含33和35个Sheet；清理清单记录必留Sheet无缺失、公式错误为0。
- 结构化来源、条款、证据定位、铜路径、11行业覆盖、因子适用性和准入门槛已经形成阶段性资产。
- 铜路径中9条标记为已核验、34条标记为待核验；新增因子自动调用为0条。

### 待规范化后再用于M5

- `CETS-002`/`CETS-003`在不同表中对水泥和铝冶炼的映射互换；来源主表还混入条款式异构记录。
- 部分证据仍使用个人Windows绝对路径，部分链接只是描述而非可访问URL。
- GB 21351-2023的28条摘录核验状态与来源整体状态须分层表达。

### 暂不采纳为项目结论

- 钢铁是第二深验行业首选候选，但尚未冻结。
- 43条命题方铜路径不等于43条国家正式条款，不能直接用于融资认定。
- 任何因子自动计算、正式碳结果、评分或授信含义均未形成。

## 03初步审计审查分类

### 已由数据初步验证，可正式采纳

- 四张企业表均为1000行、企业代号为`TF0001`—`TF1000`并一一闭合；未报告重复键、空键、孤儿键或完整重复行。
- 各表字段数量、初步字段类型、主要缺失/成对缺失、数值范围、目录复合唯一性、路径编号复用、行业/细分行业映射和主要语义一致性结果已归档。
- 三张输入表、目录规则层和参考结论层的用途边界继续有效。

### 合理但仍待确认

- 设备年限超过企业存续年限先记警告；能源来源数量不一致先作软校验；IQR异常值按行业—规模分层复核。
- 产量单位与行业的对应关系、目录覆盖缺口的补充方式、路径拼接文本的解析和人工复核流程。

### 涉及专业判断，暂不写成确定结论

- 宽表接入/长表存储、缺失状态枚举、质量规则阻断等级、目录行级`rule_id`、数值存储类型和异常值处理方法。
- 评分权重、阈值、第二深验行业、正式碳核算方法、行业基准和信贷口径。

### 与当前决策冲突

- 未发现03初步审计与当前数据边界的直接冲突。
- 任何把`转型规划结论`回灌为特征、把模拟数据写成真实业务数据或把目录候选写成正式评分的实现均与当前决策冲突。

## 下一步行动

1. 组员和02同步包含M4正式基线的最新`origin/main`并读取核心文档。
2. 03/钟先统一规范来源ID，补齐官方URL或受控原件标识，去除个人绝对路径依赖，并明确各条记录的语料/候选规则/可执行规则/因子角色。
3. 01据此形成M5知识检索开发说明；不得直接引入RAGFlow或把`转型规划结论`纳入检索语料。
4. 若翎确认M5范围并在02发送完全一致的口令“开始开发MVP”后，02才可实施M5。
5. 03继续补齐数据字典、规则来源、因子、行业基准、评分方法和第二深验行业依据。
6. M5不得提前实现M6碳核算、M7行业对标/转型行为、M8评分与信贷支持，也不处理Windows和报告扩展。

## 状态更新规则

任何成员带来正式数据、政策版本、技术变更、测试结果或范围变化时，都要区分“已验证、环境阻断、未验证、待确认”及对MVP的影响。核心治理文档由`01_项目总控与决策`统一更新；未验证功能和赛题目标不得写成已完成成果。
