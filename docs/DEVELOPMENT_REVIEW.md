# 02 软件开发成果审查与下一阶段建议

**版本**：v0.2
**日期**：2026-08-19
**审查角色**：`01_项目总控与决策`
**审查对象**：02 软件开发提交的 A—G 阶段本地开发成果
**文件性质**：开发成果审查记录，不替代 [`docs/PROJECT_PLAN.md`](PROJECT_PLAN.md)

## 1. 审查结论

当前成果已经形成一个可以在本地运行并可作为M3起点的M1/M2核心工程基线，但还不是最终产品，也不能宣称已经完成正式碳排放核算、正式评分、信贷决策建议、真实模型效果或生产部署。

本轮明确结论：

- A—F 阶段的主要本地能力已通过代码、回归测试和边界审查，正式采纳为M2核心基线；
- G 阶段只确认 macOS 本地构建成果，Windows EXE/MSI 本轮不继续；
- 已有基础报告功能暂时冻结，不进入报告制作或竞赛材料生产阶段；
- 当前网页已经具备左侧任务栏、中央对话区和右侧信息栏，但仍是信息密集型验收后台，不符合期望的 Codex 式极简工作台；
- 当前配套数据的表格角色和泄漏隔离方向是正确的，但产品叙事没有充分围绕“命题方唯一配套数据集驱动的企业评估”展开；
- 下一阶段优先级为M3 UI信息架构、React前端迁移、配套数据主流程和真实阶段状态；碳排放测算、行业对标和评分分别在后续阶段推进，而不是混入M3。

## 2. 当前验证与工作区状态

### 2.1 02 报告中已交付的内容

02 报告称已完成：

- Excel 上传、批次 ID、SHA-256、模拟数据标识和原件只读保存；
- 五张表名称、表头、类型、范围和企业代号关联校验；
- 企业详情和 2024—2025 年能耗/经营变化；
- 缺失、年度错位、设备年限、能源数量等质量提示；
- `转型目录`候选匹配、临时行级 ID 和歧义提示；
- `转型规划结论`独立对照与泄漏隔离；
- 基础 Markdown 报告生成；
- 多企业工作空间、单评估运行一企业、企业对比和独立报告；
- 多模态解析适配、外部模型会话编排和离线回退；
- macOS `.app` 和 DMG 本地构建。

### 2.2 本轮独立复核

本轮先在当前受限沙箱中重新执行：

```text
git diff --check：通过
pytest：53 passed, 1 failed, 5 warnings
```

失败项为`tests/test_desktop.py::test_desktop_smoke_entry_starts_and_stops_local_api`，错误是受限沙箱禁止`socket.bind((host, 0))`。随后在允许本机回环端口的执行环境重跑完全相同的测试套件，结果为：

```text
54 passed, 5 warnings
```

因此可确认唯一失败来自执行权限限制，不是产品业务逻辑缺陷。5条警告均为第三方PDF依赖的弃用提示，当前不阻断M2核心基线；后续依赖升级时再处理。

### 2.3 Git 状态

审查开始时工作树为`main...origin/main`，02的代码、测试、说明和打包配置仍存在未提交修改，另有Windows打包脚本未跟踪。01逐项审查后，将Windows专项说明、PowerShell脚本、构建脚本和Windows spec隔离到本地可恢复Git stash；正式基线只采纳核心M2、测试、说明、macOS默认数据封装与治理文档。原始Excel、ZIP、数据库、缓存和密钥不进入提交。

## 3. 已实现能力与未实现边界

### 3.1 已经具备的主流程

当前系统可以完成：

```text
命题方配套模拟数据
→ 企业选择
→ 企业画像基础展示
→ 五表校验与企业关联
→ 数据质量提示
→ 2024—2025 能耗变化
→ 转型目录候选
→ 参考结论对照
→ 受控会话解释
```

代码中已经体现了以下边界：

- `基本信息`、`能耗信息`、`补充信息`进入输入层；
- `转型目录`进入规则/知识层；
- `转型规划结论`保持独立参考层；
- 会话模型不能读取参考结论值，也不能修改事实、规则、因子、权重或阈值；
- 目录候选的暂定排序值不是评分；
- 当前能耗趋势只比较工作簿原始值，不输出正式碳排放量；
- 当前没有正式评分或授信通过/拒绝结论。

### 3.2 尚未实现或不能宣称的能力

- 有组织边界、Scope 1/Scope 2、排放因子和折标系数约束下的正式碳排放核算；
- 经过确认的行业基准和行业对标；
- 正式转型行为识别和路径可信度判断；
- 确认权重、阈值、标签和标准依据后的正式评分；
- 基于评分和证据的信贷支持建议；
- 真实外部模型服务的联网、额度、超时、扫描 PDF 和视觉解析完整验收；
- Windows EXE/MSI 构建、安装、升级、卸载和跨平台回归；
- 竞赛报告、PPT、答辩材料和生产分发。

## 4. 配套数据的产品定位调整

当前配套工作簿是命题方提供的脱敏、模拟生成的比赛开发测试数据，不是真实企业业务数据，也不是正式训练数据、正式评分标签或真实融资依据。

本项目后续应把它作为当前唯一的基准业务场景，而不是普通的可选上传样例：

1. 系统打开后默认进入配套数据集；
2. 用户直接从企业列表中选择企业；
3. 上传 PDF、DOCX、图片或其他文件时，将其视为当前企业的补充证据；
4. 参考结论只在“验证对照”中出现；
5. 数据来源、模拟数据声明、批次哈希和规则版本放入信息检查器，不占用中央对话空间；
6. 如果评测方之后提供其他文件，应作为新的受控数据批次或当前企业补充证据接入，不能改变配套数据的基准定位。

应用体验可以支持在同一工作空间连续评估多家企业，但内部仍应保持“一次评估运行绑定一家企业”。这样可以支持企业切换和比较，同时避免将不同企业的事实混入同一会话上下文。

## 5. 建议的最终产品主流程

最终业务闭环建议明确为：

```text
原始配套数据
→ 企业画像
→ 数据质量门
→ 能耗与经营活动数据标准化
→ 碳排放测算
→ 行业对标
→ 转型行为识别
→ 转型路径匹配
→ 转型评分
→ 风险与证据审查
→ 信贷支持建议
→ 对话解释与人工复核
```

其中：

- 目前已经跑通的是前半段的结构化数据、质量、能耗变化、目录候选和参考对照；
- 碳排放测算需要先确认组织边界、核算口径、排放因子和版本；
- 行业对标需要权威标准、适用范围和有效期；
- 评分需要 01/03 确认权重、阈值、标签和第二深验行业；
- 信贷模块只能输出审查辅助、材料清单、风险提示、贷后关注事项和转型里程碑，不自动输出授信通过或拒绝。

## 6. 最终产品模块、技术栈与接入状态

建议最终产品按 **8 个核心业务模块 + 2 个基础支撑模块** 规划。下表新增了“技术栈/已有架构”和“是否已接入、采用了什么技术”两列。

| 模块 | 主要职责 | 需要用到的技术栈或已有技术架构 | 是否已经接入、采用了什么技术 |
|---|---|---|---|
| 1. 数据批次与企业选择 | 管理配套数据、企业列表、来源、哈希和评估入口 | Python、FastAPI、`openpyxl`、SQLite、SHA-256、本地不可变原件目录、静态 HTML/CSS/JavaScript | **已接入基础版**：`main.py` 自动发现/登记默认工作簿，`m1_core.py` 负责解析，领域存储记录批次和企业代码；原始文件按本地应用目录保存 |
| 2. 企业画像与事实层 | 汇总基本信息、能耗、经营、设备、产品/服务和转型诉求 | Python 数据整形、Pydantic、SQLite、JSON 事实快照、前端信息组件 | **已接入基础版**：企业详情 API 和 `company_detail` 页面已能读取三张输入表；尚未形成完整长表事实模型 |
| 3. 数据质量与证据链 | 缺失、年度错位、类型异常、主键关联、证据位置和人工复核 | `m1_core.py` 规则校验、Pydantic、SQLite JSON、文件哈希、证据引用结构、pytest | **已接入基础版**：五表校验、企业级质量提示、附件证据位置和置信度已接入；正式数据字典和权威质量规则仍待确认 |
| 4. 能耗与碳排放测算 | 能源/活动数据标准化、排放因子、折标、Scope 边界和碳排放结果 | Python 计算服务、Decimal、版本化 `factor_registry`、规则配置、可回放计算器、证据链 | **部分接入**：已接入 2024—2025 原始值比较和缺失提示；正式碳核算计算器、因子库和边界管理尚未接入 |
| 5. 行业规则与行业对标 | 维护转型目录、行业标准、行业基准、规则版本和适用期 | JSON/YAML 行业模板、规则引擎、目录 `rule_id`、版本管理；后续可接入 SQLite/PostgreSQL | **部分接入**：已接入按行业、细分行业和产品的临时目录候选匹配；当前排序是暂定规则，不是评分，正式行业基准尚未接入 |
| 6. 转型行为与路径识别 | 识别现有转型行为、转型缺口、候选路径和证据 | 规则优先的识别器、结构化字段、补充材料解析、可选会话模型解释 | **尚未正式接入**：当前只有目录候选和文本提示，尚未形成经过确认的行为分类、路径判定或深验规则 |
| 7. 转型评分与信贷支持 | 输出评分、风险等级、材料清单、贷后关注事项和信贷支持建议 | 独立可回放评分服务、版本化权重/阈值、行业模板、证据引用、人工复核；外部模型不得直接决定结果 | **未接入**：当前明确不输出正式评分、碳排放结果或授信结论；权重、阈值、标签和信贷口径待 01/03 确认 |
| 8. 评估会话与模型调度 | 管理当前运行会话、补问、受控工具调用、解释和离线回退 | Python、FastAPI、OpenAI 兼容接口、模型能力目录、工具白名单、审计事件、离线流程 | **已接入基础版**：`orchestration/service.py`、`tools.py` 已实现运行隔离、工具白名单、停止/重试、外部模型失败回退和参考结论隔离；真实供应商联调待验收 |
| 9. 多模态材料与知识接入 | 解析 PDF、DOCX、图片、扫描件及未来政策/碳因子文档 | PyMuPDF、`pdfplumber`、`python-docx`、`openpyxl`、PaddleOCR、外部视觉模型、`evidence_ref` | **已接入适配版**：原生 PDF/DOCX/图片和外部视觉模型路由已有；真实服务、低置信度复核和长期知识库尚未完成 |
| 10. 持久化、对比与输出 | 保存工作空间、运行、附件、企业对比、基础输出和桌面入口 | SQLite、SQLAlchemy 规划架构、本地应用数据目录、Jinja2/Markdown/ReportLab 规划、PyWebView、PyInstaller | **已接入基础版，后续冻结扩展**：领域存储、运行对比、基础 Markdown 报告和 macOS `.app`/DMG 已有；报告制作与 Windows MSI 本轮不继续 |

### 6.1 当前实际技术架构

- **当前前端**：FastAPI 提供的静态 HTML/CSS/JavaScript 单页界面；尚未迁移到 React。
- **长期前端方案**：React、TypeScript、Vite、Ant Design、ECharts，是否立即迁移需单独确认。
- **当前后端**：Python、FastAPI、Pydantic、SQLite，配合 `openpyxl` 和已有 M1 核心规则。
- **模型接入**：OpenAI 兼容接口、用户自定义模型配置、能力目录、离线回退；模型用于会话调度，不是评分引擎。
- **凭据限制**：当前自定义模型API Key保存在本机应用数据目录的SQLite中，接口列表和前端不会回传密钥，但数据库内仍是本地明文存储。该方案只作为本地MVP基线接受；正式分发前应迁移到macOS Keychain、Windows Credential Manager或等价系统凭据存储，并补充文件权限、迁移和删除测试。
- **多模态**：PyMuPDF、`pdfplumber`、`python-docx`、`openpyxl`、PaddleOCR 和外部视觉模型适配。
- **桌面封装**：PyWebView、PyInstaller；macOS 已有本地构建成果，Windows 暂缓。
- **规则与核算**：应继续采用独立、可回放、可版本化的 Python 规则/计算模块，不让模型直接决定事实和评分。

## 7. UI 审查结论

当前网页已经具备左侧任务栏、中央对话区和右侧企业信息栏，但呈现方式仍偏向“功能验收后台”。中央区域同时放置上下文、数据边界、模型选择、欢迎说明、附件上传、处理过程、对话、报告和对比，右栏又同时展示多个指标和四组详细表格，导致主要任务不突出。

下一版 UI 应采用 Codex 式三栏信息架构：

### 左侧任务栏

- 工作空间；
- 企业评估运行列表；
- 当前企业；
- 新建评估；
- 最近运行；
- 侧栏边缘收起按钮。

配套数据只显示一行简要状态，哈希、批次和规则版本移到右侧检查器。

### 中央对话区

只保留当前企业、评估阶段、简洁消息、关键结果卡片、输入框、附件按钮和模型选择器。

默认隐藏：

- 原始批次 ID 和 SHA-256；
- 全部工具调用步骤；
- 大量指标卡；
- 参考结论表格；
- 详细审计事件；
- 报告生成面板。

### 右侧信息检查器

按可折叠区提供：企业画像、质量信号、能耗变化、碳排放测算、行业对标、转型路径、证据材料、参考答案对照和运行来源。

## 8. RAGFlow 判断

当前不建议把 RAGFlow 加入本地软件或当前 MVP。RAGFlow 官方定位是基于深度文档理解的开源 RAG 引擎，适合复杂文档解析、知识库检索和带引用的问答；它不是碳排放计算器、评分引擎或授信决策引擎。详见 [RAGFlow 官方 Quickstart](https://github.com/infiniflow/ragflow/blob/main/docs/quickstart.mdx)。

官方部署路径需要 Docker 和多项服务，文档列出了 x86 CPU、内存、磁盘以及 Elasticsearch/Infinity、MySQL、MinIO、Redis 等运行依赖；ARM64 镜像也不是官方默认分发路径。详见 [RAGFlow 官方 README](https://github.com/infiniflow/ragflow) 和 [Docker 配置说明](https://github.com/infiniflow/ragflow/blob/main/docker/README.md)。这与当前轻量本地网页、macOS 单机应用和离线回退目标不匹配。

当前更合理的做法是先预留：

```text
knowledge_source
knowledge_document
evidence_ref
rule_catalog
factor_registry
rule_version
factor_version
effective_date
jurisdiction
```

未来若政策、行业标准、排放因子和外部碳资料明显增多，再将 RAGFlow 作为独立后端服务接入：

```text
RAGFlow 检索证据
→ 人工/规则复核
→ 版本化规则或因子库
→ 确定性计算
→ 行业对标与评分
```

RAGFlow 的检索结果不得未经复核直接成为评分输入或信贷结论。

## 9. 下一阶段建议

本轮暂不执行 Windows 和报告制作。建议后续顺序为：

1. **M3 UI信息架构与React迁移**：形成左侧任务栏、中央简洁对话、右侧信息检查器，并保留现有运行隔离；
2. **M3配套数据优先化**：打开系统即进入配套数据集，首屏选择企业，补充文件仅作为当前企业证据；
3. **M4 Agent流程编排**：根据真实需要接入LangGraph状态运行时，并按需结合LangChain组件；
4. **M5知识检索**：先核验资料来源、版本和规模，再引入轻量检索；暂不接入RAGFlow；
5. **M6碳排放测算基础层**：建立边界、因子、版本和不可计算状态，不虚构排放结果；
6. **M7行业对标与转型行为识别**：继续维护“8行业框架＋2行业深验”，将目录规则版本化；
7. **M8评分与信贷支持**：由01/03确认权重、阈值、标签、风险等级和第二深验行业后再开发；
8. **后置交付**：报告扩展、Windows MSI、三端回归和正式分发。

## 10. 待确认事项

若翎已经确认：产品采用命题方配套数据驱动的专业Agent工作台；M3只做UI与数据主流程重构；Windows与报告暂缓；独立规则/计算模块负责事实与评分，外部模型负责会话调度和解释；暂不接入RAGFlow；继续保持“一个工作空间多企业、一次评估运行一企业”。

M3仍需02在实施中确认的仅是前端包版本、组件拆分、API最小增量和浏览器验收细节；这些技术细节不得改变领域对象、数据隔离、参考结论边界或MVP范围。

M3可以复用现有模型配置接口，但不得在浏览器状态、日志、错误提示或持久化前端存储中保存/回显API Key；系统凭据存储迁移另列为正式分发前的安全整改，不在M3擅自扩展。

## 11. 本轮变更与限制

- 新增本文件；
- 01未新增编写业务程序；本轮正式采纳的Python、HTML、测试和macOS打包改动来自02既有交付；
- 未安装依赖；
- 未启动软件开发；
- 未构建或验收 Windows EXE/MSI；
- 未扩展竞赛报告、PPT 或答辩材料；
- 本文件随M2核心基线由01统一提交和推送；Windows专项在途文件不纳入该提交；
- 本文件记录正式审查和下一阶段建议，不将目标指标、模拟数据或计划功能表述为已完成成果。
