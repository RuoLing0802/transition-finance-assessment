# 02_软件开发｜M6活动数据标准化与碳核算基础层启动提示词

- 版本：v1.0
- 日期：2026-08-22
- 负责人：`01_项目总控与决策`
- 执行者：新建独立的`02_M6活动数据与碳核算`任务
- 状态：等待若翎发送后生效
- 完整执行依据：`docs/M6_DEVELOPMENT_PROMPT.md` v1.0

## 可直接复制到新任务

```markdown
开始开发MVP

你是本项目新建的`02_M6活动数据与碳核算`开发任务。不要依赖旧02/M5任务的上下文记忆，只以当前`origin/main`和项目文件为准。

请先检查Git工作树和远端同步状态，确认当前`origin/main`已经包含M5正式集成提交；随后按项目规定顺序读取核心治理文档，并完整读取、严格执行`docs/M6_DEVELOPMENT_PROMPT.md` v1.0。

本轮只开发M6：活动数据宽转长、单位与缺失状态、版本化排放因子准入、确定性Scope 1/Scope 2基础计算、不可计算状态、证据链、运行隔离、离线回退和右侧碳核算检查器。

必须保持：

- 配套数据是脱敏模拟比赛数据，不是真实企业业务数据；
- `基本信息`、`能耗信息`、`补充信息`是输入，`转型目录`是规则/知识来源；
- `转型规划结论`不得进入活动、因子、公式、模型、特征、标签或测试预期；
- 当前生产因子自动调用仍为0条；没有03条目级冻结包和01准入确认时，只能完成M6-A，产品必须显示`not_calculable`或缺口；
- 公式测试只可使用隔离的`TEST_ONLY`因子，不得进入产品数据或公开效果；
- LangGraph只编排状态，LangChain Core只包装受控工具，模型不得创建因子、选择自由公式或修改结果；
- 不开发M7行业对标/转型行为，不开发M8评分/信贷建议，不做Windows，不扩展报告，不接RAGFlow或向量数据库。

开始编码前先向若翎报告：

1. 当前分支、HEAD、`origin/main`和工作树；
2. M5正式基线是否存在；
3. M6-A与M6-B当前能执行到哪一层；
4. 是否存在经03/01确认的因子冻结包；
5. 预计新增和修改的文件；
6. 明确不会实现的M7/M8、Windows和报告内容。

完成后运行`docs/M6_DEVELOPMENT_PROMPT.md`规定的全部测试和回归，只把本地成果交给`01_项目总控与决策`终审；不要提交，不要推送，不要修改正式`docs/`状态文件。
```
